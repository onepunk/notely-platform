# IP Whitelist for Admin Access
# Cleared - all IPs allowed at nginx level; access control enforced by application RBAC
# IP entries managed in admin_settings.system_settings (security_config.ipWhitelist)
default 1;
