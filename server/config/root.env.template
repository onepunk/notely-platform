# Notely Platform - Generated Environment Configuration
# This file is AUTO-GENERATED by configure-domains.sh
# DO NOT EDIT MANUALLY - Changes will be overwritten
#
# Generated: ${GENERATION_TIMESTAMP}
# Environment: ${ENV}

# =============================================================================
# Domain Configuration
# =============================================================================
BASE_DOMAIN=${BASE_DOMAIN}
API_DOMAIN=${API_DOMAIN}
PORTAL_DOMAIN=${PORTAL_DOMAIN}
CALENDAR_DOMAIN=${CALENDAR_DOMAIN}
DOCS_DOMAIN=${DOCS_DOMAIN}
TRACKING_DOMAIN=${TRACKING_DOMAIN}
WS_DOMAIN=${WS_DOMAIN}
GET_DOMAIN=${GET_DOMAIN}

# =============================================================================
# WebSocket Gateway Configuration
# =============================================================================
WS_GATEWAY_PORT=${WS_GATEWAY_PORT}
WS_GATEWAY_URL=wss://${WS_DOMAIN}${URL_PORT_SUFFIX}
WS_GATEWAY_SERVICE_URL=http://ws-gateway:${WS_GATEWAY_PORT}
WS_HEARTBEAT_INTERVAL_MS=${WS_HEARTBEAT_INTERVAL_MS}
WS_MAX_CONNECTIONS_PER_USER=${WS_MAX_CONNECTIONS_PER_USER}

# =============================================================================
# Service Port Configuration
# =============================================================================
API_PORT=${API_PORT}
API_HOST=${API_HOST}
AUTH_PORT=${AUTH_PORT}
USERS_PORT=${USERS_PORT}
ADMIN_CONFIG_PORT=${ADMIN_CONFIG_PORT}
ADMIN_DATABASE_PORT=${ADMIN_DATABASE_PORT}
PORTAL_PORT=${PORTAL_PORT}
CALENDAR_PORT=${CALENDAR_PORT}
WHISPER_PORT=${WHISPER_PORT}
LLM_PORT=${LLM_PORT}
GRAFANA_PORT=${GRAFANA_PORT}
LOKI_PORT=${LOKI_PORT}
DOCKER_MANAGER_PORT=${DOCKER_MANAGER_PORT}
LOGS_PORT=${LOGS_PORT}
LICENSE_PORT=${LICENSE_PORT}
SUMMARIES_PORT=${SUMMARIES_PORT}
SUPPORT_PORT=${SUPPORT_PORT}
EMAIL_PORT=${EMAIL_PORT}

# =============================================================================
# Deployment Mode Configuration
# =============================================================================
DEPLOYMENT_MODE=${DEPLOYMENT_MODE}
NGINX_HTTPS_PORT=${NGINX_HTTPS_PORT}
NGINX_HTTP_PORT=${NGINX_HTTP_PORT}
URL_PORT_SUFFIX=${URL_PORT_SUFFIX}

# =============================================================================
# External API URL Configuration
# =============================================================================
API_BASE_URL=https://${API_DOMAIN}${URL_PORT_SUFFIX}
# CORS is handled at nginx edge layer only - no application-level CORS config needed

# =============================================================================
# Service Internal URLs
# =============================================================================
SERVICE_API_INTERNAL_URL=${SERVICE_API_INTERNAL_URL}
SERVICE_PORTAL_INTERNAL_URL=${SERVICE_PORTAL_INTERNAL_URL}
SERVICE_CALENDAR_INTERNAL_URL=${SERVICE_CALENDAR_INTERNAL_URL}
SERVICE_WHISPER_URL=${SERVICE_WHISPER_URL}
SERVICE_LLM_URL=${SERVICE_LLM_URL}
LOGS_SERVICE_URL=${LOGS_SERVICE_URL}
LICENSE_SERVICE_URL=${LICENSE_SERVICE_URL}
SUMMARIES_SERVICE_URL=${SUMMARIES_SERVICE_URL}
SUPPORT_SERVICE_URL=${SUPPORT_SERVICE_URL}
LLM_GATEWAY_URL=${LLM_GATEWAY_URL}

# =============================================================================
# OAuth Callback URLs (Generated from Domain + Port + Path)
# =============================================================================
OAUTH_MICROSOFT_AUTH_CALLBACK_URL=https://${API_DOMAIN}${URL_PORT_SUFFIX}${OAUTH_MICROSOFT_AUTH_PATH}
OAUTH_MICROSOFT_CALENDAR_CALLBACK_URL=https://${API_DOMAIN}${URL_PORT_SUFFIX}${OAUTH_MICROSOFT_CALENDAR_PATH}
OAUTH_MICROSOFT_OUTLOOK_CALLBACK_URL=https://${API_DOMAIN}${URL_PORT_SUFFIX}${OAUTH_MICROSOFT_OUTLOOK_PATH}
OAUTH_GOOGLE_AUTH_CALLBACK_URL=https://${API_DOMAIN}${URL_PORT_SUFFIX}${OAUTH_GOOGLE_AUTH_PATH}
OAUTH_GOOGLE_CALENDAR_CALLBACK_URL=https://${API_DOMAIN}${URL_PORT_SUFFIX}${OAUTH_GOOGLE_CALENDAR_PATH}
OAUTH_GOOGLE_DESKTOP_AUTH_CALLBACK_URL=https://${API_DOMAIN}${URL_PORT_SUFFIX}${OAUTH_GOOGLE_DESKTOP_AUTH_PATH}

# =============================================================================
# OAuth Callback Paths
# =============================================================================
OAUTH_MICROSOFT_AUTH_PATH=${OAUTH_MICROSOFT_AUTH_PATH}
OAUTH_MICROSOFT_CALENDAR_PATH=${OAUTH_MICROSOFT_CALENDAR_PATH}
OAUTH_MICROSOFT_OUTLOOK_PATH=${OAUTH_MICROSOFT_OUTLOOK_PATH}
OAUTH_GOOGLE_AUTH_PATH=${OAUTH_GOOGLE_AUTH_PATH}
OAUTH_GOOGLE_CALENDAR_PATH=${OAUTH_GOOGLE_CALENDAR_PATH}
OAUTH_GOOGLE_DESKTOP_AUTH_PATH=${OAUTH_GOOGLE_DESKTOP_AUTH_PATH}

# =============================================================================
# Portal OIDC Configuration
# =============================================================================
AUTH_ENABLE_OIDC_PROVIDER=${AUTH_ENABLE_OIDC_PROVIDER}
AUTH_ENABLE_DESKTOP_OAUTH=${AUTH_ENABLE_DESKTOP_OAUTH}
AUTH_DESKTOP_STATE_TTL_SECONDS=${AUTH_DESKTOP_STATE_TTL_SECONDS}
AUTH_DESKTOP_LOGIN_TEMPLATE_VERSION=${AUTH_DESKTOP_LOGIN_TEMPLATE_VERSION}
AUTH_DESKTOP_DEFAULT_SCOPE=${AUTH_DESKTOP_DEFAULT_SCOPE}
AUTH_OIDC_PORTAL_CLIENT_ID=${AUTH_OIDC_PORTAL_CLIENT_ID}
AUTH_OIDC_PORTAL_CLIENT_SECRET=${AUTH_OIDC_PORTAL_CLIENT_SECRET}
AUTH_OIDC_DEFAULT_SCOPE=${AUTH_OIDC_DEFAULT_SCOPE}
OIDC_PORTAL_CLIENT_ID=${AUTH_OIDC_PORTAL_CLIENT_ID}
OIDC_PORTAL_CLIENT_SECRET=${AUTH_OIDC_PORTAL_CLIENT_SECRET}
OIDC_PORTAL_REDIRECT_URI=https://${PORTAL_DOMAIN}${URL_PORT_SUFFIX}/oauth/callback
OIDC_AUTHORITY_URL=https://${API_DOMAIN}${URL_PORT_SUFFIX}/api/oauth
NEXT_PUBLIC_OIDC_CLIENT_ID=${AUTH_OIDC_PORTAL_CLIENT_ID}
NEXT_PUBLIC_OIDC_SCOPE=${AUTH_OIDC_DEFAULT_SCOPE}
NEXT_PUBLIC_OIDC_REDIRECT_URI=https://${PORTAL_DOMAIN}${URL_PORT_SUFFIX}/oauth/callback
NEXT_PUBLIC_OIDC_AUTHORITY=https://${API_DOMAIN}${URL_PORT_SUFFIX}/api/oauth

# =============================================================================
# Database Configuration
# =============================================================================
POSTGRES_DB=${POSTGRES_DB}
POSTGRES_USER=${POSTGRES_USER}
POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
POSTGRES_HOST=${POSTGRES_HOST}
POSTGRES_PORT=${POSTGRES_PORT}

# Database URL for connection
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# Connection Pool Configuration
DB_POOL_MAX=${DB_POOL_MAX}
DB_POOL_MIN=${DB_POOL_MIN}
DB_POOL_IDLE_TIMEOUT_MS=${DB_POOL_IDLE_TIMEOUT_MS}
DB_POOL_CONNECTION_TIMEOUT_MS=${DB_POOL_CONNECTION_TIMEOUT_MS}
DB_POOL_ACQUIRE_TIMEOUT_MS=${DB_POOL_ACQUIRE_TIMEOUT_MS}
DB_STATEMENT_TIMEOUT_MS=${DB_STATEMENT_TIMEOUT_MS}
DB_QUERY_TIMEOUT_MS=${DB_QUERY_TIMEOUT_MS}

# Service-specific database users
POSTGRES_AUTH_USER=${POSTGRES_AUTH_USER}
POSTGRES_AUTH_PASSWORD=${POSTGRES_AUTH_PASSWORD}
POSTGRES_USERS_USER=${POSTGRES_USERS_USER}
POSTGRES_USERS_PASSWORD=${POSTGRES_USERS_PASSWORD}
POSTGRES_CALENDAR_USER=${POSTGRES_CALENDAR_USER}
POSTGRES_CALENDAR_PASSWORD=${POSTGRES_CALENDAR_PASSWORD}
POSTGRES_MEETINGS_USER=${POSTGRES_MEETINGS_USER}
POSTGRES_MEETINGS_PASSWORD=${POSTGRES_MEETINGS_PASSWORD}
POSTGRES_TRANSCRIPTS_USER=${POSTGRES_TRANSCRIPTS_USER}
POSTGRES_TRANSCRIPTS_PASSWORD=${POSTGRES_TRANSCRIPTS_PASSWORD}
POSTGRES_NOTES_USER=${POSTGRES_NOTES_USER}
POSTGRES_NOTES_PASSWORD=${POSTGRES_NOTES_PASSWORD}
POSTGRES_SUMMARIES_USER=${POSTGRES_SUMMARIES_USER}
POSTGRES_SUMMARIES_PASSWORD=${POSTGRES_SUMMARIES_PASSWORD}
POSTGRES_ADMIN_SERVICE_USER=${POSTGRES_ADMIN_SERVICE_USER}
POSTGRES_ADMIN_SERVICE_PASSWORD=${POSTGRES_ADMIN_SERVICE_PASSWORD}
POSTGRES_ACTIONS_USER=${POSTGRES_ACTIONS_USER}
POSTGRES_ACTIONS_PASSWORD=${POSTGRES_ACTIONS_PASSWORD}
POSTGRES_PORTAL_USER=${POSTGRES_PORTAL_USER}
POSTGRES_PORTAL_PASSWORD=${POSTGRES_PORTAL_PASSWORD}
POSTGRES_ADMIN_DATABASE_USER=${POSTGRES_ADMIN_DATABASE_USER}
POSTGRES_ADMIN_DATABASE_PASSWORD=${POSTGRES_ADMIN_DATABASE_PASSWORD}
POSTGRES_SUPPORT_USER=${POSTGRES_SUPPORT_USER}
POSTGRES_SUPPORT_PASSWORD=${POSTGRES_SUPPORT_PASSWORD}
POSTGRES_EMAIL_USER=${POSTGRES_EMAIL_USER}
POSTGRES_EMAIL_PASSWORD=${POSTGRES_EMAIL_PASSWORD}

# Admin database service configuration
ADMIN_DATABASE_ALLOWED_SCHEMAS=${ADMIN_DATABASE_ALLOWED_SCHEMAS}
ADMIN_DATABASE_DEFAULT_DB=${ADMIN_DATABASE_DEFAULT_DB}
ADMIN_DATABASE_MAX_PAGE_SIZE=${ADMIN_DATABASE_MAX_PAGE_SIZE}
ADMIN_DATABASE_DEFAULT_PAGE_SIZE=${ADMIN_DATABASE_DEFAULT_PAGE_SIZE}

# =============================================================================
# Redis Configuration
# =============================================================================
REDIS_HOST=${REDIS_HOST}
REDIS_PORT=${REDIS_PORT}
REDIS_PASSWORD=${REDIS_PASSWORD}
REDIS_DB=${REDIS_DB}
REDIS_KEY_PREFIX=${REDIS_KEY_PREFIX}
REDIS_MAX_RETRIES=${REDIS_MAX_RETRIES}
REDIS_CONNECT_TIMEOUT_MS=${REDIS_CONNECT_TIMEOUT_MS}
REDIS_COMMAND_TIMEOUT_MS=${REDIS_COMMAND_TIMEOUT_MS}

# Redis URL for connection
REDIS_URL=redis://:${REDIS_PASSWORD}@${REDIS_HOST}:${REDIS_PORT}/${REDIS_DB}

# =============================================================================
# RabbitMQ Configuration
# =============================================================================
RABBITMQ_HOST=${RABBITMQ_HOST}
RABBITMQ_PORT=${RABBITMQ_PORT}
RABBITMQ_MANAGEMENT_PORT=${RABBITMQ_MANAGEMENT_PORT}
RABBITMQ_USER=${RABBITMQ_USER}
RABBITMQ_PASSWORD=${RABBITMQ_PASSWORD}
RABBITMQ_VHOST=${RABBITMQ_VHOST}
RABBITMQ_ERLANG_COOKIE=${RABBITMQ_ERLANG_COOKIE}
RABBITMQ_URL=amqp://${RABBITMQ_USER}:${RABBITMQ_PASSWORD}@${RABBITMQ_HOST}:${RABBITMQ_PORT}/${RABBITMQ_VHOST}
RABBITMQ_MANAGEMENT_URL=http://${RABBITMQ_HOST}:${RABBITMQ_MANAGEMENT_PORT}

# =============================================================================
# Caching Configuration
# =============================================================================
CACHE_TTL_SHORT=${CACHE_TTL_SHORT}
CACHE_TTL_MEDIUM=${CACHE_TTL_MEDIUM}
CACHE_TTL_LONG=${CACHE_TTL_LONG}
CACHE_TTL_USER_PROFILE=${CACHE_TTL_USER_PROFILE}
CACHE_TTL_CALENDAR_EVENTS=${CACHE_TTL_CALENDAR_EVENTS}
CACHE_TTL_MEETINGS=${CACHE_TTL_MEETINGS}
CACHE_ENABLED=${CACHE_ENABLED}
CACHE_KEY_PREFIX=${CACHE_KEY_PREFIX}

# =============================================================================
# Rate Limiting Configuration
# =============================================================================
RATE_LIMIT_WINDOW_MS=${RATE_LIMIT_WINDOW_MS}
RATE_LIMIT_MAX_REQUESTS=${RATE_LIMIT_MAX_REQUESTS}
RATE_LIMIT_AUTH_MAX=${RATE_LIMIT_AUTH_MAX}
RATE_LIMIT_AUTH_WINDOW_MS=${RATE_LIMIT_AUTH_WINDOW_MS}
RATE_LIMIT_API_MAX=${RATE_LIMIT_API_MAX}
RATE_LIMIT_API_WINDOW_MS=${RATE_LIMIT_API_WINDOW_MS}
RATE_LIMIT_REDIS_ENABLED=${RATE_LIMIT_REDIS_ENABLED}
RATE_LIMIT_WHITELIST_IPS=${RATE_LIMIT_WHITELIST_IPS}

# =============================================================================
# Logging Configuration
# =============================================================================
LOG_LEVEL=${LOG_LEVEL}
LOG_FORMAT=${LOG_FORMAT}
LOG_TIMESTAMP=${LOG_TIMESTAMP}
LOG_REQUESTS=${LOG_REQUESTS}
LOG_RESPONSES=${LOG_RESPONSES}
LOG_SLOW_REQUESTS=${LOG_SLOW_REQUESTS}
LOG_SLOW_THRESHOLD_MS=${LOG_SLOW_THRESHOLD_MS}
LOG_REDACT_FIELDS=${LOG_REDACT_FIELDS}

# =============================================================================
# Security Configuration
# =============================================================================
JWT_SECRET=${JWT_SECRET}
JWT_PRIVATE_KEY=${JWT_PRIVATE_KEY}
JWT_PUBLIC_KEY=${JWT_PUBLIC_KEY}
JWT_KEY_ID=${JWT_KEY_ID}
JWT_EXPIRES_IN=${JWT_EXPIRES_IN}
ENCRYPTION_KEY=${ENCRYPTION_KEY}
# Service-to-service authentication
GATEWAY_SERVICE_API_KEY=${GATEWAY_SERVICE_API_KEY}
# Internal API key for CI/CD pipeline and internal service communication
INTERNAL_API_KEY=${INTERNAL_API_KEY}
# Sync service admin API key
SYNC_ADMIN_API_KEY=${SYNC_ADMIN_API_KEY}
# Backup encryption key (AES-256-CBC)
BACKUP_ENCRYPTION_KEY=${BACKUP_ENCRYPTION_KEY}
# HMAC-SHA256 key for signed release download URLs
RELEASES_SIGNING_KEY=${RELEASES_SIGNING_KEY}
# CORS handled at nginx edge layer - see services/nginx/templates/*.conf.template
SESSION_TTL_SECONDS=${SESSION_TTL_SECONDS}
SESSION_COOKIE_NAME=${SESSION_COOKIE_NAME}
SESSION_SECURE=${SESSION_SECURE}
SECURITY_HEADERS_ENABLED=${SECURITY_HEADERS_ENABLED}
SECURITY_HSTS_ENABLED=${SECURITY_HSTS_ENABLED}
SECURITY_CSP_ENABLED=${SECURITY_CSP_ENABLED}
INPUT_VALIDATION_ENABLED=${INPUT_VALIDATION_ENABLED}
INPUT_SANITIZATION_ENABLED=${INPUT_SANITIZATION_ENABLED}

# =============================================================================
# External Service Timeouts
# =============================================================================
HTTP_CLIENT_TIMEOUT_MS=${HTTP_CLIENT_TIMEOUT_MS}
HTTP_CLIENT_RETRY_MAX=${HTTP_CLIENT_RETRY_MAX}
HTTP_CLIENT_RETRY_DELAY_MS=${HTTP_CLIENT_RETRY_DELAY_MS}
GRAPH_API_TIMEOUT_MS=${GRAPH_API_TIMEOUT_MS}
GRAPH_API_RETRY_MAX=${GRAPH_API_RETRY_MAX}
GOOGLE_API_TIMEOUT_MS=${GOOGLE_API_TIMEOUT_MS}
GOOGLE_API_RETRY_MAX=${GOOGLE_API_RETRY_MAX}
WHISPER_API_TIMEOUT_MS=${WHISPER_API_TIMEOUT_MS}
WHISPER_API_RETRY_MAX=${WHISPER_API_RETRY_MAX}
LLM_API_TIMEOUT_MS=${LLM_API_TIMEOUT_MS}
LLM_API_RETRY_MAX=${LLM_API_RETRY_MAX}

# =============================================================================
# Graceful Shutdown Configuration
# =============================================================================
SHUTDOWN_TIMEOUT_MS=${SHUTDOWN_TIMEOUT_MS}
SHUTDOWN_DRAIN_REQUESTS=${SHUTDOWN_DRAIN_REQUESTS}

# =============================================================================
# Health Check Configuration
# =============================================================================
HEALTH_CHECK_ENABLED=${HEALTH_CHECK_ENABLED}
HEALTH_CHECK_DB_ENABLED=${HEALTH_CHECK_DB_ENABLED}
HEALTH_CHECK_REDIS_ENABLED=${HEALTH_CHECK_REDIS_ENABLED}

# =============================================================================
# Microsoft OAuth Credentials
# =============================================================================
MICROSOFT_CLIENT_ID=${MICROSOFT_CLIENT_ID}
MICROSOFT_CLIENT_SECRET=${MICROSOFT_CLIENT_SECRET}
MICROSOFT_TENANT=${MICROSOFT_TENANT}
MICROSOFT_OIDC_SCOPE=${MICROSOFT_OIDC_SCOPE}

# Microsoft Mailer App
MAIL_APP_CLIENT_ID=${MAIL_APP_CLIENT_ID}
MAIL_APP_CLIENT_SECRET=${MAIL_APP_CLIENT_SECRET}
MAIL_TENANT_ID=${MAIL_TENANT_ID}
EMAIL_FROM=${EMAIL_FROM}
EMAIL_FROM_NAME=${EMAIL_FROM_NAME}
MICROSOFT_SENDER_EMAIL=${MICROSOFT_SENDER_EMAIL}

# =============================================================================
# Google OAuth
# =============================================================================
GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
GOOGLE_REDIRECT_URI=${GOOGLE_REDIRECT_URI}

# =============================================================================
# External Services
# =============================================================================
HUGGINGFACE_TOKEN=${HUGGINGFACE_TOKEN}
# HUGGING_FACE_HUB_TOKEN is an alias for compatibility with vLLM and other HuggingFace tools
HUGGING_FACE_HUB_TOKEN=${HUGGINGFACE_TOKEN}
PIP_INDEX_URL=${PIP_INDEX_URL}
PIP_EXTRA_INDEX_URL=${PIP_EXTRA_INDEX_URL}
PIP_TRUSTED_HOST=${PIP_TRUSTED_HOST}
CUSTOM_CA_PEM=${CUSTOM_CA_PEM}

# =============================================================================
# Azure Communication Services (ACS)
# =============================================================================
ACS_ENDPOINT=${ACS_ENDPOINT}
ACS_RESOURCE_ID=${ACS_RESOURCE_ID}
ACS_CONNECTION_STRING=${ACS_CONNECTION_STRING}
ACS_DISPLAY_PREFIX_DEFAULT=${ACS_DISPLAY_PREFIX_DEFAULT}
ACS_CAPTIONS_LANG=${ACS_CAPTIONS_LANG}
ACS_TOKEN_TTL_MINUTES=${ACS_TOKEN_TTL_MINUTES}
ACS_FROM_NUMBER=${ACS_FROM_NUMBER}
ACS_TRANSCRIPTION_LOCALE=${ACS_TRANSCRIPTION_LOCALE}
ACS_JOINER_CALLBACK_URI=${ACS_JOINER_CALLBACK_URI}
ACS_JOINER_WEBHOOK_SECRET=${ACS_JOINER_WEBHOOK_SECRET}
ACS_JOINER_MAX_CONCURRENT_JOBS=${ACS_JOINER_MAX_CONCURRENT_JOBS}
ACS_JOINER_POLL_SECONDS=${ACS_JOINER_POLL_SECONDS}
ACS_JOINER_PUBLIC_BASE_URL=${ACS_JOINER_PUBLIC_BASE_URL}
ACS_CAPTION_SEGMENT_LIMIT=${ACS_CAPTION_SEGMENT_LIMIT}
ACS_JOINER_FORWARD_URL=${ACS_JOINER_FORWARD_URL}
ACS_JOINER_FORWARD_TIMEOUT_MS=${ACS_JOINER_FORWARD_TIMEOUT_MS}
ACS_JOINER_LOG_LEVEL=${ACS_JOINER_LOG_LEVEL}
ACS_SERVICE_ACCOUNT_EMAIL=${ACS_SERVICE_ACCOUNT_EMAIL}
ACS_SERVICE_ACCOUNT_PASSWORD=${ACS_SERVICE_ACCOUNT_PASSWORD}
ACS_IDENTITY_CLIENT_ID=${ACS_IDENTITY_CLIENT_ID}
ACS_IDENTITY_CLIENT_SECRET=${ACS_IDENTITY_CLIENT_SECRET}
ACS_IDENTITY_TENANT_ID=${ACS_IDENTITY_TENANT_ID}
ACS_SERVICE_ACCOUNT_OBJECT_ID=${ACS_SERVICE_ACCOUNT_OBJECT_ID}
TEAMS_ACS_CALLBACK_SECRET=${TEAMS_ACS_CALLBACK_SECRET}

# =============================================================================
# Calendar Auto-Sync Configuration
# =============================================================================
CALENDAR_AUTO_SYNC_ENABLED=${CALENDAR_AUTO_SYNC_ENABLED}
CALENDAR_AUTO_SYNC_CRON=${CALENDAR_AUTO_SYNC_CRON}
CALENDAR_SYNC_MAX_USERS_PER_RUN=${CALENDAR_SYNC_MAX_USERS_PER_RUN}
CALENDAR_SYNC_USER_DELAY=${CALENDAR_SYNC_USER_DELAY}
CALENDAR_SYNC_THROTTLE_BACKOFF=${CALENDAR_SYNC_THROTTLE_BACKOFF}

# =============================================================================
# Teams Meeting Join Configuration
# =============================================================================
TEAMS_DEFAULT_JOIN_MODE=${TEAMS_DEFAULT_JOIN_MODE}
USE_ACS_BY_DEFAULT=${USE_ACS_BY_DEFAULT}
TEAMS_JOIN_MAX_ATTEMPTS=${TEAMS_JOIN_MAX_ATTEMPTS}
TEAMS_JOIN_LEAD_SECONDS=${TEAMS_JOIN_LEAD_SECONDS}
TEAMS_JOIN_CLAIM_HOLD_MINUTES=${TEAMS_JOIN_CLAIM_HOLD_MINUTES}
TEAMS_JOIN_STALE_MINUTES=${TEAMS_JOIN_STALE_MINUTES}
TEAMS_CALLBACK_TOKEN_TTL_MINUTES=${TEAMS_CALLBACK_TOKEN_TTL_MINUTES}

# =============================================================================
# Observability (Loki / Grafana)
# =============================================================================
LOKI_RETENTION_DAYS=${LOKI_RETENTION_DAYS}
LOKI_BASE_URL=${LOKI_BASE_URL}
GRAFANA_ADMIN_USER=${GRAFANA_ADMIN_USER}
GRAFANA_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD}
# Grafana public URL - accessed via nginx proxy at /grafana/
GRAFANA_ROOT_URL=${GRAFANA_ROOT_URL}
NEXT_PUBLIC_GRAFANA_URL=${NEXT_PUBLIC_GRAFANA_URL}
GRAFANA_INTERNAL_URL=${GRAFANA_INTERNAL_URL}

# =============================================================================
# Node.js Configuration
# =============================================================================
NODE_OPTIONS=${NODE_OPTIONS}
NODE_ENV=${NODE_ENV_VALUE}

# =============================================================================
# CrowdSec - Intrusion Prevention System
# =============================================================================
CROWDSEC_API_KEY=${CROWDSEC_API_KEY}
CROWDSEC_BOUNCER_API_KEY=${CROWDSEC_BOUNCER_API_KEY}

# =============================================================================
# Diagnostics Analysis Service
# =============================================================================
ANALYSIS_SERVICE_API_KEY=${ANALYSIS_SERVICE_API_KEY}

# =============================================================================
# Google reCAPTCHA
# =============================================================================
RECAPTCHA_SECRET_KEY=${RECAPTCHA_SECRET_KEY}

# =============================================================================
# SSL Configuration
# =============================================================================
SSL_MODE=${SSL_MODE}
SSL_CERT_PATH=${SSL_CERT_PATH}
SSL_KEY_PATH=${SSL_KEY_PATH}
LETSENCRYPT_EMAIL=${LETSENCRYPT_EMAIL}
LETSENCRYPT_STAGING=${LETSENCRYPT_STAGING}

PORTAL_URL=https://${PORTAL_DOMAIN}${URL_PORT_SUFFIX}
GET_URL=https://${GET_DOMAIN}${URL_PORT_SUFFIX}
